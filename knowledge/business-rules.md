# Cross-cutting Business Rules

Rules that span more than one entity. Entity-specific rules live in that entity's file under `entities/`.
Each rule carries a source tag (`documented` / `observed` / `assumed`), the session, and a date — e.g. `(observed, session_03, 2026-06-24)`. See `INSTRUCTIONS.md`.

## Business rules

- _(none captured yet)_
